/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "ec06ccb";
export const BOUNCE_BUILD_NOTE =
  "BEST uses ESPN last-5 + your max per-leg. If you still see “≤ $1.40 shorts only” or seed form like (28, 15, 19, 17, 22), this site is on an old deploy.";
