/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "shared-board-cache";
export const BOUNCE_BUILD_NOTE =
  "Sportsbet boards use shared 12-min Data Cache (same prices, fewer Odds API credits). Stamp must read shared-board-cache.";
