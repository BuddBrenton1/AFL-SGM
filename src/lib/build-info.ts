/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "jumper-archive";
export const BOUNCE_BUILD_NOTE =
  "SGM legs show jumper + club colour; finished games/trades move to Archived. Stamp must read jumper-archive.";
