/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "best-sb-only";
export const BOUNCE_BUILD_NOTE =
  "BEST uses ESPN last-5 + live book prices only (no Bounce model fill-ins). If every leg is ~$1.17 with no SB badge, this site is on an old deploy.";
