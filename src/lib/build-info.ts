/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "l5-all-multis";
export const BOUNCE_BUILD_NOTE =
  "L5 hit badges on every player-prop leg (target SGMs + BEST). Stamp must read l5-all-multis.";
