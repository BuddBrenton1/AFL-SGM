/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "l5-board-names";
export const BOUNCE_BUILD_NOTE =
  "L5 badges resolve ESPN form by Sportsbet player name (not just seed roster). Stamp must read l5-board-names.";
