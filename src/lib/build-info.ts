/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "sgm-season-diversity";
export const BOUNCE_BUILD_NOTE =
  "SGMs pruned by season/L5 form (one line per player+market) + diversified card. Stamp must read sgm-season-diversity.";
