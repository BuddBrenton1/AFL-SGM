/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "l5-sb-only-toggle";
export const BOUNCE_BUILD_NOTE =
  "Scanner checkbox: 5/5 SB only — every target leg must be L5 5/5 with a live book price. Stamp must read l5-sb-only-toggle.";
