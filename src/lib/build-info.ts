/** Bumped on each scanner ship so the live UI proves which build is deployed. */
export const BOUNCE_BUILD = "sb-quota-fix";
export const BOUNCE_BUILD_NOTE =
  "Live SB prices need a working Odds API key with credits. Quota exhausted = no SB badges. Footer stamp must read sb-quota-fix.";
